# {{ project_name }}

This project was bootstrapped from the Dark Stack template.

## Structure

- `backend/`: Django project.
{% if include_frontend -%}
- `frontend/`: Ionic project.
{% endif -%}
- `docker-compose-dev.yaml`: local PostgreSQL service.
{% if use_asgi -%}
- ASGI/Daphne is enabled for the backend.
{% endif -%}
{% if use_celery -%}
- Celery is enabled with Redis broker and Django-backed results/beat.
{% endif -%}

## Quick Start

```bash
uv sync
docker compose -f docker-compose-dev.yaml up -d
uv run python backend/manage.py migrate
uv run python backend/manage.py runserver
```

{% if use_celery -%}
## Celery

```bash
cd backend
./launch_dev_celery.sh
```
{% endif -%}

{% if include_frontend -%}
## Frontend

```bash
cd frontend
npm install
npm start
```
{% endif -%}
