[project]
name = "{{ project_slug }}"
version = "0.1.0"
description = "{{ project_name }}"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.3",
    "psycopg-pool>=3.3.0",
    "psycopg[binary]>=3.3.3",
]
