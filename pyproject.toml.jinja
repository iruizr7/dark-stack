[project]
name = "{{ project_slug }}"
version = "0.1.0"
description = "{{ project_name }}"
requires-python = ">=3.14"
dependencies = [
{% if use_asgi -%}
    "channels[daphne]>=4.3.2",
{% endif -%}
    "django>=6.0.3",
    "psycopg-pool>=3.3.0",
    "psycopg[binary]>=3.3.3",
]
